# ラカイン旅々(Rakhine Explorer)要件定義書

> 本ドキュメントは、実装しながら固めてきた仕様を事後的に整理したものです。
> `src/data/spots.ts` など複数のソースコード内コメントから参照されています。

## 1. 目的

ミャンマー・ラカイン州の観光情報(史跡・自然・食・伝統文化)を、ミャンマー語/英語/日本語で
紹介するカタログサイト。「その土地で何が有名か」を訪問前に把握できることを目指す
(例: 沖縄の観光サイトで海ブドウ・三線が紹介されているような体験)。

## 2. 対応言語

- ミャンマー語(`my`、既定言語)
- 英語(`en`)
- 日本語(`ja`)

UI文言は `src/i18n/dictionary.ts` に集約。3言語とも用意できないデータ(Google由来の
場所名など)は、取得できた言語にフォールバックする。

## 3. カテゴリー構成

サイトはホーム画面のタブで4カテゴリーに分かれる。**カテゴリーごとに全く異なる
コンテンツ設計**になっている点に注意(単純な「場所検索」の使い回しではない)。

| カテゴリー | 内部ID | 内容 | 地図 |
|---|---|---|---|
| Temples & Heritage | `temple` | 史跡・寺院。キュレーション済みデータ + Google Places検索結果 | あり |
| Coast & Nature | `coast` | ビーチ・滝のみ(ホテル・博物館等は除外) | あり |
| Food | `food` | **お店検索ではなく、ラカインの名物料理そのものの紹介** | なし |
| Traditional | `craft`(内部ID) | **工房検索ではなく、ラカインを象徴する伝統的な物の紹介**(衣装・工芸・芸能) | なし |

Food/Traditionalは「その土地で何が有名か」を伝える特集ページであり、地図や
お店検索とは切り離している。

## 4. データソースと取得方針

### 4.1 キュレーション済みスポット(`src/data/spots.ts`)

実在が確認できている史跡(ミャウーUの寺院群、ンガパリビーチ等)を中心に、手動で
入力した少数のデータ。食・工芸カテゴリーの一部エントリはサンプルデータであることを
`sourceNote` で明記している。

### 4.2 Google Places API (New)

`temple`/`coast`カテゴリーで、その場のリアルタイム検索結果を表示する
(`src/app/api/spots/route.ts`)。

- **検索クエリは常に英語で送信する。** UIの表示言語に合わせて`languageCode`を
  変えると検索精度が大きく落ちる不具合をGoogle側で確認済み(有名スポットが
  結果から消え、無関係な施設が混入した)。
- **表示名だけ、選択言語で個別に再取得する。** Place Details APIを場所ごとに
  追加で1回呼び、UIの言語に合わせた名前を表示する(検索のヒット件数には影響させない)。
- 住所からGoogleのPlus Code(位置コード)を正規表現で除去し、読みやすくしている
  (`src/lib/formatAddress.ts`)。
- coastカテゴリーは、種類が`beach`またはタイトルに"beach"/"waterfall"を含むものだけに
  絞り込み、ホテル・博物館等を除外する。
- 固有名の無い(カテゴリー名そのままの)登録は、写真も取れない場合のみ一覧から除外する。

**重要な前提**: このAPIキーは、Google Cloud側で**請求先アカウントがリンクされていないと
`photos`フィールドが一切返ってこない**(他のフィールドは正常に返るため気づきにくい)。
運用時は請求先アカウントのリンク状態を必ず確認すること。無料トライアル中は
明示的に「アップグレード」しない限り課金は発生しない。

### 4.3 Wikipedia / Wikimedia Commons(写真・説明文の補完)

Google側に写真が無い場合のフォールバックとして、`src/lib/wikipediaPhoto.ts` で
Wikipedia記事・Wikimedia Commonsの実写真を検索する。

- **あいまい検索はしない。** 転写ゆれ(例: "Shittaung" ⇔ Wikipedia側は
  "Shite-thaung")が多く、あいまい検索では無関係な同名記事(例: 別都市の同名寺院)を
  誤って拾うリスクが高いため、次の2つの「完全一致」のみを使う:
  1. Google Place ID(安定した識別子)をキーにした、目視確認済みの対応表
  2. スポット名(英語)そのもの、または「スポット名, 地域名」の完全一致
- 誤って無関係な記事を拾わないよう、記事の`description`にマンダレー・ヤンゴン等
  **別地域名が含まれる場合は無条件で除外**し、`description`/`extract`に
  Rakhine/Arakan/Mrauk等のラカイン関連語が無ければ採用しない(リレバンスチェック)。
- 失敗理由(記事なし/画像フィールドなし/別地域と判定/リクエストエラー)は
  必ず`console.error`に出す(404で記事が単に存在しないケースのみログを抑制)。
- スポット詳細ページの説明文も、Wikipediaの「要約API」(1文)ではなく
  「導入部全体を返すAction API」を使い、より詳しい説明を表示する(`src/lib/wikipedia.ts`)。

### 4.4 ユーザー投稿(Supabase `user_posts`テーブル)

ログインユーザーが`/post`から投稿できるコンテンツ。カテゴリーは
`food` / `craft`(お店・工房、位置必須) / `dish`(料理紹介、位置任意) /
`traditional`(伝統文化紹介、位置任意)。

- `dish`カテゴリーの投稿は Foodタブに表示される(`src/components/DishList.tsx`)。
- `traditional`カテゴリーの投稿は Traditionalタブに、Wikipedia由来の紹介と
  並べて表示される(`src/components/TraditionalList.tsx`)。
- 投稿者本人には編集・削除ボタンが表示される(RLSの delete/update ポリシーにより
  実際に操作できるのも本人の投稿のみ)。
- カテゴリー値はDB側のCHECK制約(`user_posts_category_check`)で管理されており、
  新しいカテゴリーを追加する場合はSupabase側でこの制約も更新する必要がある。

## 5. 地図の仕様

`src/components/RakhineIllustrationMap.tsx` — OpenStreetMapのタイルは使わず、
`src/data/rakhineBoundary.geo.json`(Nominatimから取得した実際の州境界を簡略化した
GeoJSON)を単色のSVGイラストとして描画する、沖縄観光サイトのような見た目の地図。

- 緯度経度→SVG座標の変換は `src/lib/mapProjection.ts`(緯度で経度方向を補正する
  簡易投影)。外部APIやライブラリは使わない、完全無料の実装。
- スポットは丸ドット+ラベルで表示。評価が高い順に処理し、近接して重なるラベルは
  自動的に間引く(ドット自体は必ず残す)。
- ポップアップは表示しない(ピンや他のピンを隠してしまうため)。クリックすると
  選択状態になり、詳細は地図の下のパネル(`SelectedPlaceDetail`)に表示される。
- 拡大縮小・パン操作は無い(州全体を常に一望できる一枚絵)。

**例外**: `/post`投稿時に位置を選ぶ`LocationPicker.tsx`は、実際の地図上でピンを
ドラッグして正確な座標を選ぶ必要があるため、引き続きLeaflet + OpenStreetMapタイルを
使用する(このコンポーネントだけ実地図)。

## 6. 非機能要件・技術構成

- Next.js (App Router) + TypeScript
- 地図: 自作SVGイラスト地図(一覧画面) / react-leaflet + OpenStreetMap(位置選択のみ)
- 認証・DB・ストレージ: Supabase
- 外部API: Google Places API (New)(要課金設定)、Wikipedia/Wikimedia Commons API(無料)
- Google Places検索結果は24時間キャッシュ(`Next.js`の`fetch`キャッシュ、
  コスト抑制とGoogle利用規約の両立のため)

## 7. 既知の制限・今後の課題

- キュレーション済みスポットの一部(食・工芸カテゴリー)はサンプルデータで、
  実在店舗の特定情報ではない(`sourceNote`参照)。
- Wikipedia/Commonsで確認できないスポットは、写真が付かずプレースホルダー表示のまま
  (誤った写真を出すより正直な表示を優先する方針)。
- 検索ボックス(`q`パラメータ)での絞り込みは英語名との一致判定のため、
  非英語での検索語入力には対応していない。
- イラスト地図は州全体を常に一望する設計のため、拡大縮小・詳細な道路情報は無い。
